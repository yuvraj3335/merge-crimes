Review the following tracker change for consistency with the actual cycle outcome.

## Original tracker state (before Codex ran)

{original_tracker}

## Proposed tracker diff (what Codex changed)

{diff}

## Cycle context

Phase: {phase}
Slice attempted: {slice_attempted}

Files changed:
{files_changed}

Validations passed: {validations_passed}

Validation commands run:
{validation_commands}

---

## Your task

Determine whether the tracker changes are consistent with what actually happened.

Be strict.  The tracker must not:
- Claim a milestone or slice is complete if validations failed.
- Advance the phase counter without evidence of completed milestones.
- Remove or overwrite work that is still in progress.
- Fabricate timestamps or completion markers unsupported by the evidence above.

Return ONLY a JSON object with exactly this structure (no markdown, no explanation
outside the JSON):

{{
  "is_consistent": true,
  "issues": [],
  "recommendation": "accept",
  "reasoning": "brief explanation"
}}

or

{{
  "is_consistent": false,
  "issues": ["specific issue 1", "specific issue 2"],
  "recommendation": "revert",
  "reasoning": "brief explanation"
}}

Valid values for "recommendation": "accept" or "revert".
