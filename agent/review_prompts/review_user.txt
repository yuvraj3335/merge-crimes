Generate a Codex CLI review prompt for the following code review task.
Your output will be passed verbatim to Codex running in full-auto mode.
Write the prompt that Codex will execute — do NOT do the review yourself.

---

WORKSTREAM TO REVIEW: {workstream}

SCOPE DESCRIPTION:
{scope_description}

TARGET FILES (Codex should read and inspect these):
{scope_files_excerpt}

---

INVENTORY CONTEXT (codebase size reference for the prompt):
{inventory_excerpt}

---

ARCHITECTURE MAP (import edges — use to scope dead_code and architecture searches):
{arch_map_excerpt}

---

PRIOR FINDINGS TO SKIP (tell Codex to avoid duplicating these):
{prior_findings}

---

OPEN RISKS (include as context in the generated prompt):
{open_risks}

---

TRACKER SUMMARY:
{tracker_summary}

---

REPO ROOT: {repo_root}

---

Write a thorough Codex review prompt (target 900–1200 words) that:

1. Names the specific files to read (from the TARGET FILES list above)
2. Instructs Codex to focus on the "{workstream}" workstream category
3. Tells Codex to verify every finding by actually reading the file and running rg/grep
4. For dead_code: requires Codex to show the rg search command + output confirming
   zero cross-file references before claiming a symbol is dead
5. Tells Codex to skip anything that duplicates the PRIOR FINDINGS listed above
6. Requires "edge_cases" in each finding with ≥2 entries grounded in the actual code
7. Instructs Codex to output {{"findings": []}} if the scope is clean
8. Reminds Codex: read-only review — do NOT change any files, do NOT run browser:smoke
9. Instructs Codex to look for SYSTEMIC patterns: if the same class of issue appears
   in 5+ locations, report it as ONE high-severity systemic finding with all locations
   listed in the evidence field — do not report each instance separately
10. Instructs Codex to go DEEP beyond surface exports:
    - Duplicate logic: same calculation or transformation implemented twice
    - Reimplemented interfaces: logic that should live in shared/ but is copied
    - State ownership violations: state split across modules that should own it centrally
    - Missing abstractions: repeated branching patterns that should be a single utility
    - Architectural violations: wrong-direction imports, layering breaches
    - Feature correctness: logic bugs, silent failures, wrong business logic, missing edge
      case handling, incorrect state transitions, off-by-one errors, incorrect conditionals
    - Performance: N+1 patterns, unnecessary re-renders, missing memoization, blocking ops,
      over-fetching, inefficient loops or data structures
    - Code quality: SOLID violations, SRP breaches, god objects/files that are too large to
      reason about, principle violations (DRY, YAGNI, LoD)

Be concrete: name the files, name the patterns to look for, give Codex specific rg
search strategies for this workstream. Not generic boilerplate.
